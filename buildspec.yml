version: 0.2

phases:
  install:
    runtime-versions:
      python: 37
    commands:
      - pip install --upgrade pip
      - pip install --upgrade awscli
      - pip install --upgrade boto3      
  build:
    commands:
      - pwd
      - ls -l
      - python ./test.py